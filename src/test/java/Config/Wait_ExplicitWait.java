package Config;


import BaseClass.OpenBrowser;
//import org.junit.Assert;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class Wait_ExplicitWait {
 public static void waitUntilVisibility(WebElement content1){


// Explicit Wait
    // WebDriverWait explicitWait = new WebDriverWait(OpenBrowser.driver, 10);
   //  explicitWait.until(ExpectedConditions.visibilityOf(content1));

   //  Assert.assertTrue("Content is not displayed", content1.isDisplayed());
   //  System.out.println(content.getText());

    }

public static void waitToClick(){
//        WebDriverWait wait = new WebDriverWait(driver,50);
//        WebElement UploadSuccess =wait.until(ExpectedConditions.elementToBeClickable(By.xpath("//div[@class='alert alert-info ng-star-inserted']")));
//        String txt=UploadSuccess.getText();
//        Assert.assertTrue(txt.contains("File Uploaded Successfully"));}
        }

}
